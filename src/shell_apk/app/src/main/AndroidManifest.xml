<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Existing -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Batch 1 — Screen -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Batch 2 — Alarm -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Batch 2 — Audio service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Silent background activity starts (used by iappyx.intent.launchApp from triggers).
         Defaults OFF per-app — no install-time dialog, no functional effect until the user
         explicitly enables "Display over other apps" in Settings for the specific app. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Needed for iappyx.intent.isAppInstalled() / launchApp() to see arbitrary packages
         on Android 11+. Sideloaded app — Play Store policy on this permission doesn't apply. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Batch 2 — Contacts & SMS -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Batch 2 — Calendar -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Batch 2 — Biometric -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Batch 2 — NFC -->
    <uses-permission android:name="android.permission.NFC" />

    <!-- Audio recording + WebView getUserMedia -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Network state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- mDNS multicast for NSD bridge -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- WiFi Direct P2P -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <!-- Bluetooth LE -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Do Not Disturb -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Wallpaper -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <!-- Read from Downloads (Android 9 and below) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.fingerprint" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:label="IAPPYX_PLACEHOLDER_LABEL_XXXXXXXXXXXX"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.NoTitleBar"
        android:hardwareAccelerated="true">

        <activity
            android:name="com.iappyx.generated.placeholder.ShellActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTop"
            android:showOnLockScreen="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- NFC -->
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- Share target: receive shared text and images from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- Notification action receiver -->
        <receiver
            android:name="com.iappyx.generated.placeholder.NotificationActionReceiver"
            android:exported="false" />

        <!-- Scheduled notification receiver -->
        <receiver
            android:name="com.iappyx.generated.placeholder.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- Alarm receiver — exported=true needed for BOOT_COMPLETED broadcast -->
        <receiver
            android:name="com.iappyx.generated.placeholder.AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Scheduled background tasks -->
        <service
            android:name="com.iappyx.generated.placeholder.TaskService"
            android:exported="false"
            android:foregroundServiceType="shortService" />
        <receiver
            android:name="com.iappyx.generated.placeholder.TaskSchedulerReceiver"
            android:exported="false" />

        <!-- Home screen widget — disabled by default, enabled on first widget.update() -->
        <receiver
            android:name="com.iappyx.generated.placeholder.WidgetProvider"
            android:exported="false"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Background audio service -->
        <service
            android:name="com.iappyx.generated.placeholder.AudioService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <service
            android:name="com.iappyx.generated.placeholder.NetworkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        <service
            android:name="com.iappyx.generated.placeholder.PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Disable Firebase auto-init — we init programmatically -->
        <meta-data android:name="firebase_messaging_auto_init_enabled" android:value="false" />
        <meta-data android:name="firebase_analytics_collection_enabled" android:value="false" />

        <!-- Foreground location tracking service -->
        <service
            android:name="com.iappyx.generated.placeholder.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Geofence transition receiver (legacy iappyx.location.addGeofence) -->
        <receiver
            android:name="com.iappyx.generated.placeholder.GeofenceReceiver"
            android:exported="false" />

        <!-- Geofence transition receiver for iappyx.trigger.geofence (Play Services) -->
        <receiver
            android:name="com.iappyx.generated.placeholder.GeofenceTransitionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.iappyx.trigger.GEOFENCE_TRANSITION" />
            </intent-filter>
        </receiver>

        <!-- Trigger bridge: broadcast-based triggers (charger, headphones).
             Static registration needed for charger/headphones because they must fire even
             when the process is dead. BT/WiFi are dynamic-only (registered from ShellActivity
             and BOOT_COMPLETED via TriggerReceiver.registerDynamic). -->
        <receiver
            android:name="com.iappyx.generated.placeholder.TriggerReceiver"
            android:exported="true">
            <!-- Charger + headphones: exempt from background-broadcast restrictions,
                 work from a static receiver on all API levels. -->
            <intent-filter>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
                <action android:name="android.intent.action.HEADSET_PLUG" />
            </intent-filter>
            <!-- Bluetooth ACL connect/disconnect: still allowed to static receivers
                 on API 26+. WiFi NETWORK_STATE_CHANGED is NOT in the exempt list, so
                 WiFi triggers are dynamic-only (registered when ShellActivity runs).
                 Caveat: after a reboot, WiFi triggers won't fire until the user opens
                 the app once. -->
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
            </intent-filter>
            <!-- Exempt-list broadcasts: allowed to manifest receivers even from a cold
                 process. Give these a fallback static entry so they can wake the app
                 before the user has opened it post-install. -->
            <intent-filter>
                <action android:name="android.intent.action.AIRPLANE_MODE" />
                <action android:name="android.intent.action.BATTERY_LOW" />
                <action android:name="android.intent.action.BATTERY_OKAY" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Keeps the app process alive so dynamic TriggerReceiver registrations survive
             the user swiping the app away. Started on-demand when a trigger is registered
             with persistent:true; stopped when the last persistent trigger is cancelled. -->
        <service
            android:name="com.iappyx.generated.placeholder.TriggerKeepaliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-configured event triggers for automation" />
        </service>

        <!-- FileProvider for camera + share -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
